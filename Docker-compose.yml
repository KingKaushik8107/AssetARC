version: '3.8'

services:
  # 1. MySQL DB Service
  mysqldb:
    image: mysql:8.0
    container_name: assetarc-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: assetarc_db
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql #
    networks:
      - assetarc-network
  # 2. Spring Boot Backend Service
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: assetarc-backend
    restart: on-failure
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysqldb:3306/assetarc_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
    depends_on:
      - mysqldb
    networks:
      - assetarc-network


  # 3. React Frontend Service
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: assetarc-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - assetarc-network

networks:
  assetarc-network:
    driver: bridge

volumes:
  mysql-data: # 


